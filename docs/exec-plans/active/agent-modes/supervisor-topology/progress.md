# Agent Modes — Supervisor Topology — Progress

Status tracking for the Supervisor Topology track ([plan.md](./plan.md)).
Sibling track: [Planning Primitive](../../../completed/agent-modes/planning-primitive/progress.md).
Statuses: `Not started` · `In progress` · `Done` · `Blocked`.

| Task | Description | Status | Note |
|---|---|---|---|
| S1 | API: `topology` (immutable) + `preset` + `supervisor` sub-object; validation; canonicalisation | Done | `SupervisorConfigRequest` + `AgentConfigRequest` fields + `ConfigValidationHelper` + `AgentService` immutability gate; 35 new `SupervisorConfigValidationTest` cases + 11 new `AgentServiceTest` cases; all 513 Java unit tests pass |
| S2 | `PresetDefaults` bundles applied at creation; `research` low-concurrency default | Done | `PresetDefaults.java` (5 preset bundles) + `ConfigValidationHelper.validatePreset` + `AgentService.createAgent` seeding + `taskTimeoutSeconds` on `AgentConfigRequest`. `canonicalizeConfig` carry-through of preset-injected tools is gated on `config.preset() != null` and restricted to `PresetDefaults.PRESET_INJECTED_TOOLS`, so the **no-preset path is byte-for-byte unchanged from pre-S2 (e61337d)** — verified. 69 `PresetDefaultsTest` cases incl. 3 no-preset pinning tests. All Java+Console tests pass. **TaskService submission-time fallback for `agent_config.task_timeout_seconds` — WIRED** (follow-up task, same branch). Resolution order: (1) explicit `task_timeout_seconds` on the submission request wins; (2) `agent_config.task_timeout_seconds` read via `ConfigValidationHelper.getAgentTaskTimeoutSeconds` (one minimal DB fetch, reusing the same `AgentRepository` path as `isAgentMemoryEnabled`); (3) `DEFAULT_TASK_TIMEOUT_SECONDS=3600` platform default. Malformed / absent agent-config value falls through to (3), never throws. 3 new `TaskServiceTest` cases + 6 new `ConfigValidationHelperTest` cases (incl. PGobject regression, null config, malformed JSON). E7 closed end-to-end: a `research`-preset task submitted without an explicit timeout gets 14400 s, not 3600 s. |
| S3 | Shared fan-out helper (`run_subagent`): isolated context + ceiling + heartbeat + timeout + depth cap | Done | **The shared primitive S4 and S6 both depend on — built once, neither driver forks it.** `executor/subagents/fanout.py` (`run_subagent`, `SubagentResult`, `SubagentCeiling`, `MAX_SUBAGENT_DEPTH=2`, `SUBAGENT_HEARTBEAT_EVENT`, `filter_headless_tools`, `build_subagent_node`) + `executor/subagents/__init__.py`. Sub-agent = compiled ReAct subgraph **node reached by `Send`, sharing the parent checkpointer** (NOT an imperative `ainvoke`); isolated `sub_messages` channel (parent `messages` never sees inner turns); token+turn ceiling / wall-clock timeout / depth-cap → **returned `SubagentResult` failure markers, never raises**; headless filter drops `request_human_input` before `bind_tools`; `subagent.heartbeat` emitted via injected callable as a **span event only** (no `task_events` row, no `lease_expiry` touch, no `core/heartbeat.py` call). 16 fake-model unit tests (`services/worker-service/tests/test_subagent_fanout.py`) on `MemorySaver`, all green. **Postgres durability confirmed** (repo-root `tests/backend-integration/test_subagent_fanout_durability.py`, 2 tests via `make e2e-test`): per-inner-turn resume + namespaced-transcript persistence against the real `PostgresDurableCheckpointer`. **Checkpointer fix landed:** the custom `PostgresDurableCheckpointer` could not round-trip a pending `Send` on the `__pregel_tasks` channel (`langchain_dumps`/`loads` raised `NotImplementedError` on load) — fixed to route that one channel through the JsonPlus `serde` (CLAUDE.md message round-trip invariant preserved for all other channels); regression-guarded by `tests/test_checkpointer.py::...test_send_in_pregel_tasks_channel_round_trips`. Required by S6's structural `Send` fan-out. |
| S4 | `dispatch_subagent` ReAct tool wrapping the helper | Done | **Topology-1 driver over S3's `run_subagent` — does NOT fork the machinery.** `tools/subagent_tools.py` (LLM-facing `dispatch_subagent(prompt, tools, budget)` schema only — `depth` deliberately absent from the schema; `budget_to_ceiling` maps the turn budget → `SubagentCeiling`, clamped). `executor/graph.py`: (1) allowlist-gated registration in `_get_tools` mirroring the `plan_write`/memory-tool gate (counts against `MAX_TOOLS_PER_AGENT`, no exemption); (2) **graph wiring** — a thin `subagent` driver node that calls `run_subagent` and threads its `SubagentResult` back as a `ToolMessage` keyed to the original `tool_call_id` (success → summary; `ceiling`/`timeout`/`error`/`depth` failure marker → descriptive `ToolMessage`, never a raised graph error); the **post-agent routing edge** (`route_after_agent`) splits a turn — each `dispatch_subagent` call → `Send("subagent", {tool_call_id, prompt, tools, budget, depth=state_depth+1})`, all other calls → the ToolNode, in the same super-step. `depth` is sourced from graph state (default 0) and incremented — **never from LLM args** (a crafted `args.depth` is ignored). The ToolNode wrapper **strips dispatch calls before invoking `ToolNode`** so each `tool_call_id` is answered exactly once (the dispatch tool body is an unreachable stub — it must be `Send`-routed, never run in the ToolNode). Isolation preserved — the parent `messages` channel only ever receives the summary `ToolMessage`; the sub-agent's inner turns stay on S3's separate channel. The sub-agent's tool set is bounded to the subset of the parent's tools the LLM named (it can never use a tool the parent lacks; `dispatch_subagent` itself is never delegated; S3's headless filter independently drops interrupt-bearing tools). **Mechanism re-verified against `langgraph==1.0.5`** (throwaway spike): a conditional edge returning a mixed `[Send(...), "tools"]` list dispatches both in one super-step with no unanswered-tool-call error. 27 fake-model unit tests (`tests/test_dispatch_subagent_tool.py`, `MemorySaver`, no ports/subprocess) — gating on/off, Send-routing + arg pass-through (`budget→ceiling`) + depth-from-state, depth-not-LLM-escalatable, mixed-turn every-id-answered-once (compiled end-to-end), success→keyed `ToolMessage` + isolation, each failure marker→`ToolMessage`, tool-cap respected, Pattern-B grep clean. Full no-infra worker suite green (1108 passed). Note: heartbeat `emit` is a log no-op for now — full Langfuse span wiring is S9-adjacent. |
| S5 | Supervisor Scope node: clarity + conditional clarify + immutable brief | Done | New `executor/supervisor/` package (`__init__.py`, `state.py`, `nodes.py::scope_node`, `prompts.py` Scope template only — additive for S7). **`SupervisorState` subclasses `RuntimeState` (`total=False`)** — strict superset, inherits every channel verbatim + adds `brief` (write-once, scope_node), `iteration` (`_max_reducer`, seed 0 / S6 increments), `subtasks` (`list[dict]`, seed [] / S6 writes), `subagent_results` (**keyed-by-`subtask` reducer `_merge_subagent_results`**, idempotent — S6 merges, S5 declares), `findings` (`operator.add` — S7 appends). **Named reducers** (builtin `max`/`dict.update` fail LangGraph 1.0.5 signature introspection — verified). `scope_node` (async): reads query from `state["messages"]`, LLM clarity assessment, **conditional clarify gate honoring `supervisor.scope_clarification_enabled`** — true+ambiguous → `interrupt({"type":"input",...})` reusing the existing `waiting_for_input` task-status path (NOT a parallel channel), `Command(resume=...)` value folded into the brief; false → never interrupts (best-effort brief); brief **write-once** every path; seeds `iteration=0`/`subtasks=[]`. Flag read from the loaded `agent_config` snapshot via `config["configurable"]` (no API re-fetch); absent → v1 default `True` (clarify on; headless opts out). **S8 must inject `scope_model` + `agent_config` into the runnable config** when compiling the Supervisor graph. Spike-verified against `langgraph==1.0.5`: superset compiles, `interrupt()`/`Command(resume=...)` work from a node, all four gate outcomes. 10 unit tests (`tests/test_supervisor_scope.py`, fake-model, no infra, worktree-safe) + harness run via `make worker-test` (16 selected, isolated DB), full worker suite 1091 passed/100 skipped. Does NOT edit `graph.py` (S8 wires it). |
| S6 | Supervisor node + structural `Send` fan-out + iteration + `subagent_results` reducer | Done | **Topology-2 structural driver over S3's `run_subagent` — reuses `build_subagent_node`, does NOT fork it.** `executor/supervisor/nodes.py::supervisor_node` (async): reads `brief`/`iteration`/`subagent_results`; LLM iteration-decision via new `prompts.py::build_supervisor_prompt` (structured `{decision, subtasks:[{prompt,subtask?,redispatch?}], reason}`, parsed — never freeform; fail-safe to `stop` on parse error); **mints `subtask` ids deterministically as `f"{iteration}.{index}"` — NEVER trusted from the LLM (§A11-E8)**, carry-forward only on explicit `redispatch:true` naming an existing FAILED id (so the reducer overwrites that marker in place); clamps to `max_fanout_per_iteration` (truncate + cap-reason `supervisor_iteration` event, no silent truncation); bounded by `max_iterations` (cap forces `stop` + cap-reason event); **partial-failure gate — raises `SupervisorAllFailedError` ONLY on zero-progress (a round returned with no success anywhere); ≥1 success proceeds**. New `executor/supervisor/graph.py::add_supervisor_fanout` supplies the Supervisor↔fan-out↔Supervisor sub-wiring: a `_fanout_edge` conditional edge `Send`s each subtask (`depth=1`, Supervisor is depth 0) to `_fanout_node` which calls the **shared `run_subagent`** and writes `{subagent_results:{<subtask>:result-dict}}` into S5's keyed reducer; a `_gather_node` at the **fan-out super-step boundary** (runs after all `Send` branches merge — never mid-branch, §A11-E2) then the iteration conditional edge routes `continue→supervisor` / `stop→writer_node_name|END`. **Seams left:** `writer_node_name` (S7's Writer; `None`→`END` for now), and S8 owns the `_build_graph` topology branch + `durability="sync"` compile + cost loop + config injection (`supervisor_model`/`agent_config`/`supervisor_emit`/`supervisor_fanout_deps`). **D1 (deferred, §A8): `_maybe_log_oversized_payload` logs a `checkpoint.oversized` warning past 1 MB — log only, NO cap/truncation.** New `core/subagent_events.py::emit_supervisor_iteration` is the stable emit-helper interface S6 calls (structured-log stub today; **S9 wires it to `_insert_task_event` + migration `0025` CHECK value `supervisor_iteration`**). State: added `supervisor_decision` channel (last-write-wins). 14 fake-model/fake-`run_subagent` unit tests (`tests/test_supervisor_fanout.py`, `MemorySaver`+`durability="sync"`, no ports/subprocess) — parsed-subtask emit, deterministic minting, **within-iteration collision (distinct from cross-round idempotency, §A11-E8)**, clamp+cap-event, `max_iterations` stop, event-payload shape, partial-failure proceed vs. zero-return fail, reducer idempotency + carry-forward overwrite, N-way `Send` dispatch, **crash resume-forward (completed sibling restored not recomputed, only unfinished branch re-runs)**, oversized-payload log threshold. Verified via `make worker-test PYTEST_ARGS='-k "supervisor_fanout or subagent_results or iteration"'` (16 passed, isolated DB). Pattern-B grep clean (`parent_task_id`/`sub_agent_id`/`waiting_for_subagent` — none). Send mechanism re-verified against `langgraph==1.0.5` via throwaway spike (fan-out + keyed reducer + crash-resume-forward). |
| S7 | Subagent findings contract + one-shot Writer + citation binding + verify pass | Done | **Closes the Deep Research graph: structured findings → one-shot Writer → deterministic citation binding.** **Findings-into-state wiring:** S6's `_fanout_node` now wraps each subtask prompt in S7's `prompts.py::build_subagent_prompt` (Subagent findings template — instructs `{"findings":[{claim,source_url,supporting_quote}]}` JSON, NOT freeform prose; quotes copied VERBATIM) so the sub-agent's `SubagentResult.summary` carries structured findings; on `result.ok` it calls `nodes.py::parse_findings(summary, iteration, subtask, emit)` and returns `{"findings":[...]}` **alongside** the unchanged `{"subagent_results":{...}}` write into S5's append-only `findings` channel (`operator.add` concatenates every branch). A failed/empty sub-agent contributes `[]` (failure marker unaffected). **`parse_findings`** (async): parses the summary (fail-safe `[]` on non-JSON, drops quote-less entries — uncitable), **mints `finding_id` deterministically** as `f"{subtask}-{sha1(subtask,index,claim,quote)[:8]}"` (stable + run-unique, NEVER trusted from the model — mirrors §A11-E8), and emits a **`subagent_finding`** event `{iteration,subtask,finding_id,source_url}` per finding via the new `core/subagent_events.py::emit_subagent_finding` helper (claim/quote ride the Langfuse span, not the row — §A7; stub-forwarding to injected sink, S9 wires the migration `0025` CHECK value + insert body). **`citations.py` (new):** `resolve(finding_id, findings)` — deterministic dict lookup, **NO LLM**; hit → `{finding_id,source_url,supporting_quote,claim}`; miss → render error flag `{finding_id,error:RENDER_ERROR_FLAG}` (NEVER a fabricated source, §A5). `verify(report, findings, *, model)` — **one verify call per distinct cited finding, run CONCURRENTLY** (bounded `asyncio.Semaphore(8)`, deduped by `extract_citations`, ≤`WRITER_FINDINGS_CAP`): per cited sentence asks the verify model "does this quote support this sentence?" → per-citation `{finding_id,supported:bool}` flags (first-appearance order preserved via `gather`); unknown id → render error flag (no LLM call; flagged, not verified/fabricated); **never rewrites the report, never invents a source** (fail-safe to `supported` on unparseable reply). **Each per-citation call is exception-contained — a flaky call (timeout/rate-limit/transport) degrades fail-safe for that one citation (`supported:True` + soft `verify_error` note) rather than sinking the already-generated report; `gather` never raises out.** **`writer_node` (async, one-shot):** `reduce_findings` → SINGLE `writer_model.ainvoke` over reduced corpus + brief honoring `writer_style` (`formal_report`\|`annotated_bullets`, v1 default `formal_report`) citing by `finding_id` ONLY (no inline URLs) → `citations.verify` → `citations.resolve` each cited id; returns `{report, citations, verify_flags}` (new last-write-wins state channels). **NOT parallel section-writing** (one call site — the design's load-bearing reason). **D2 (deferred, §A8/§A12 ledger) — v1 Writer-context reduction:** `reduce_findings` is a **HARD CAP** at clearly-commented open-decision flag `WRITER_FINDINGS_CAP=50` — keeps the first N by the `findings` channel's append order (earliest-iteration/sub-agent/finding; documented), drops the rest, `log()`s dropped ids+count (no silent truncation, §A7). **Select/reorder ONLY — `supporting_quote` NEVER mutated** (slices the same dicts → byte-identical quotes; the core S7 invariant §A0 inv.4). Map-reduce summarization / Track-7-on-Writer-input are the DEFERRED alternatives (commented pointer); D2 ledger row stays **OPEN** (gated on S11 cap-hit-rate + report-quality). **Seam left for S8:** `writer_node` is the iteration `stop` target (S6 left `writer_node_name=None→END`); S8 adds the node + terminal edge, compiles `durability="sync"`, and injects `writer_model`/`verify_model`/`agent_config` into config. 16 fake-model unit tests (`tests/test_supervisor_citations.py`, no infra, worktree-safe): structured-findings parse + minted ids (stable, subtask-scoped, unparseable→`[]`), `subagent_finding` event per finding, **immutability — corpus>cap through reduction, every surviving quote byte-identical (unicode)**, resolve hit + miss(render-flag-not-fabrication), verify flags-unsupported+passes-supported (content-keyed fake — concurrency-safe) + unknown-id-not-invented + **verify-call-failure-degrades-gracefully (one citation's call raises → flagged fail-safe, report + other flags survive)**, writer cites-by-id-only/one-shot/style-switch, reduction caps+logs+identity-under-cap, findings-into-state integration (success→findings+events / failure→`[]`). 3 S6 fan-out tests updated for the now-wrapped subtask prompt (assert containment, not equality). Full worker unit suite **1154 passed / 100 skipped** (skips pre-existing infra-gated). Pattern-B grep clean. No DB/API/migration changes (S9 owns the `0025` CHECK + projection); narrow unit scope is the correct coverage. |
| S8 | `_build_graph` topology branch + `durability="sync"` + cost-attribution mechanism + super-step-boundary budget pause | Done | **Integration capstone — the Supervisor topology is now reachable AND metered (E1/E2).** **(1) `_build_graph` topology branch** (`executor/graph.py`): `agent_config.get("topology","react")` → `react`/absent = existing ReAct build byte-for-byte (preserved; regression-guarded by `test_graph_topology_branch.py` + the full ReAct DB suite green); `supervisor` → new `executor/supervisor/graph.py::build_supervisor_graph` (START→scope→supervisor⇄fan-out→writer→END); **unknown topology → `ValueError` (defensive, NOT a silent ReAct fallback)**. Returns an uncompiled `StateGraph`; `execute_task` compiles it with the shared `PostgresDurableCheckpointer` and astreams `durability="sync"` (unchanged compile site). **(2) Dep injection** — `_inject_supervisor_configurable` binds `scope_model`/`supervisor_model`/`writer_model`/`verify_model` (v1: all the agent's configured model), `agent_config`, `supervisor_emit` (structured-log sink; S9 wires `task_events`+migration 0025), `iteration`, and `supervisor_fanout_deps` (`model`/`checkpointer`/`ceiling`/`tools`/`emit` — S6's fail-fast satisfied). **(3) Cost-attribution mechanism (§A11-E1, the highest-risk point) — ⚠ spec's `subgraphs=True` path DISPROVEN:** a throwaway spike vs `langgraph==1.0.5` showed the outer `astream(stream_mode="updates", subgraphs=True)` does NOT descend into `run_subagent`'s nested `ainvoke` (the sub-agents never appear as namespace events, and `updates` only yields a node's return value). Built the spec's **option (b) — helper-accumulated usage through state**: a new `step_usage` channel (`SupervisorState`, additive `merge_usage` reducer); every LLM-bearing node (`scope_node`/`supervisor_node`/`writer_node`, incl. a `UsageAccumulatingModel` wrapper capturing `citations.verify` spend) accumulates its `usage_metadata` into `step_usage`; `run_subagent` now surfaces accumulated sub-agent usage on `SubagentResult.usage`, and `_fanout_node` folds it into `step_usage`. The extended cost loop (`_attribute_supervisor_event_cost`) reads `step_usage` off each astream event and writes it **ADDITIVELY** to the parent super-step `checkpoint_id` via `add_cost_and_preserve_metadata` (new `_record_supervisor_step_cost`; the ReAct `_record_step_cost` overwrite path is untouched). **Pattern A: NO `sub_agent_id` column, NO per-sub-agent rows, NO per-tree rollup, NO refund.** New leaf module `executor/usage.py` (shared by `subagents`+`supervisor`, no circular dep). **No supervisor node named `"agent"`** (the E1 guardrail). **(4) Super-step-boundary budget pause (§A11-E2):** spend is recorded on every event (additive — billed once incl. each fan-out branch), but `_check_budget_and_pause` is evaluated ONLY at a super-step boundary (the `supervisor_gather` node or a single-node scope/supervisor/writer step) — NEVER on a `supervisor_fanout` event (which would strand in-flight siblings). Accepts the design-budgeted `max_fanout × ceiling` overshoot (§A8/D3 finer-metering deferred — noted, not built). The `:3160` carve-out skips are NOT widened. **E7 NOT built here** (S2's `research`-preset `task_timeout_seconds=14400` owns it). **(5) Durability fix surfaced by integration:** `run_subagent` used a non-UUID derived `thread_id` (`{parent}:subagent:{id}`) that worked on MemorySaver but FAILS the custom `PostgresDurableCheckpointer` (maps thread_id→task_id::uuid + lease-gates writes). Fixed: sub-agents now run on the **parent UUID thread_id + a per-subtask `checkpoint_ns`** (both `_fanout_node` and S4's `subagent_node` updated; new optional `checkpoint_ns` param on `run_subagent`). **Tests:** unit (pinned venv, all green incl. full suite **1173 passed/100 skipped**): `test_supervisor_cost.py` (9), `test_supervisor_node_usage.py` (3), `test_graph_topology_branch.py` (4), `+2` in `test_subagent_fanout.py` (SubagentResult.usage), `+1` in `test_supervisor_fanout.py` (fan-out folds usage). **DB (`make e2e-test`):** new `tests/backend-integration/test_supervisor_fanout_budget.py` — **wide-fan-out NON-ZERO cost** (5-way fan-out, fake models w/ known tokens → exact **18 420 µ$** at the parent task under `model_token_spend`, asserted NO `sub_agent_id` column) + **boundary-pause billing-each-sibling-once** (pause fires only after all 5 siblings stream, exactly 7 ledger rows, cumulative 13 260 µ$, no double-bill). Existing ReAct/budget/crash/HITL/subagent-durability DB suites (43 tests) all still green. **Resume follow-up (spec-review-reclassified, NOT an S8 defect):** the E2 boundary contract + bill-once is proven on real Postgres; a spec-review repro confirmed the **graph layer resumes CLEANLY** on the real `PostgresDurableCheckpointer`. The remaining unverified link is an **`execute_task`-level config-reinjection** item (resume must re-run `_inject_supervisor_configurable` after `_build_graph` rebuilds the supervisor nodes) — **owned by S11** (see the S11 row's PREREQUISITE note: a live paused-task resume test). |
| S9 | Migration `0025` + worker event emission + `ActivityProjectionService` tree mapping | Done (both halves merged) | **Worker+migration half:** `0025_agent_modes_subagent_events.sql` additively admits the four fan-out markers (`subagent_started`/`subagent_finding`/`subagent_failed`/`supervisor_iteration`) — full 21-value `0024` allowlist + 4 = 25, canonical `task_events_event_type_check`, via the **weaker-lock E10 path** (`ADD ... NOT VALID` → `VALIDATE` → `DROP` old → `RENAME _v2`). Pattern A — no new columns; `iteration`(int, **1-based**, matches S6's reducer)/`subtask`(str `"<iter>.<idx>"`) ride in `details`. **`subagent_heartbeat` deliberately NOT admitted** (Langfuse span event, §A11-E4). CI glob picks up `0025`. Worker emit reconciliation (did NOT fork S6/S7): `core/subagent_events.py` keeps the `emit_*(emit, ...)` surface S6/S7 call + ADDS `emit_subagent_started` (truncates `prompt_preview` to 200) + `emit_subagent_failed`; S8's injected `supervisor_emit` log no-op is replaced by the REAL `build_task_event_sink(pool, task_id, tenant_id, agent_id)` (per-emit own connection → `_insert_task_event`, signature unchanged). **At-least-once, NOT atomic with checkpoint write**; `(event_type,iteration,subtask)` is the dedup key. The real sink drops `subagent.heartbeat` (carried on the same `emit`) via `SINK_ADMITTED_EVENT_TYPES` so it never hits the CHECK. New emits wired in S6's `_fanout_node` (`subagent_started` at dispatch, `subagent_failed` on failure marker) — observability-only. 12 worker tests + 36 regression. **API-projection half:** `ActivityEventResponse` + optional `iteration`/`subtask` (`@JsonInclude NON_NULL`); `mapMarker` 4 cases → `marker.subagent.started/.finding/.failed` + `marker.supervisor.iteration`; `USER_VISIBLE_MARKERS` adds the 3 user-visible kinds (`started` excluded as lifecycle telemetry); **duplicate-tolerance** via a `LinkedHashMap` keyed on `(event_type, iteration, subtask)` in `getActivity` (first-wins `subagent_started`, last-wins result-bearing) — projection is a **flat list, Console groups round→sub-agent** (skeleton only, NOT sub-agent transcripts — E5); `default -> null` forward-compat preserved. 12 new projection tests (38 in `ActivityProjectionServiceTest`; full Java 603, 0 failures). **Follow-up (tracked):** `subagent.heartbeat` → Langfuse span sink (§A11-E4) is still NOT implemented — it's been log-no-op→dropped through S3/S4/S9; the wedged-branch-detector span needs the per-task Langfuse client wired in `execute` (S4 flagged it "S9-adjacent"). Non-blocking for correctness; observability gap to close before research GA. |
| S10 | Console: preset selector (locked on edit) + supervisor section + sub-agent tree | Done | **Console surface for Deep Research — three pieces, all read-only-consuming S1 (`agent_config.{topology,preset,supervisor}`) + S9 (four fan-out markers).** **(1) Preset selector (`CreateAgentDialog.tsx`):** `<select data-testid="agent-config-preset">` with `chat`(default)/`coding`/`investigation`/`research`; the `research` option's visible label is **"Deep Research"**. Derivation `topologyForPreset(preset)` → `research`→`supervisor`, else `react`; rendered read-only as `data-testid="agent-config-topology"` ("Mode: Deep Research"/"ReAct"). Payload sends `agent_config.preset` always; **does NOT synthesize `topology`** (S1's `AgentService.canonicalizeTopology` derives it server-side from preset — confirmed against the merged Java). **(2) `SupervisorConfigSection.tsx` (new, mirrors `ContextManagementSection`):** five fields in order with the spec testids — `supervisor-config-max-fanout` (number [1,20]), `-max-iterations` (number [1,10]), `-source-allowlist` (chip input, cap 50 + inline "Maximum 50 entries"), `-writer-style` (`<select>` "Formal report"/"Annotated bullets" → enum `formal_report`/`annotated_bullets`), `-scope-clarification` (checkbox). `applicable` gate (`return null` when false) + `disabled` prop for read-only-on-edit. Payload nests under `agent_config.supervisor`, only-send-what-was-set (`supervisorDirty` ref, mirrors `ctxMgmtDirty`; unset section → no `supervisor` key). **"Supervisor" never appears in visible copy** — only in testids/payload keys (asserted by a `/supervisor/i` text scan test). **(3) Read-only-on-edit (`AgentDetailPage.tsx`, invariant #2):** preset/topology render as read-only `<span>`s (both view AND edit mode — a span is structurally un-editable, the strongest form of invariant #2; the earlier disabled-`<select>` was simplified per code review, dropping a duplicate-option + unlabeled-control nit) + a static note "Mode is fixed at agent creation; create a new agent to change it." — **NO change handler** anywhere. The `supervisor` sub-object renders via `<SupervisorConfigSection disabled applicable>` on both view + edit (conservative — no S1 contract for mutating these post-creation; `updateAgent` only gates topology). **(4) Sub-agent tree (`ActivityPane.tsx`):** extended `ActivityEventKind` + `ActivityEvent.{iteration,subtask}` in `types/index.ts`; the four kinds = `marker.supervisor.iteration` `{iteration,subtasks_emitted,decision,reason}`, `marker.subagent.started` `{iteration,subtask,prompt_preview,...}`, `marker.subagent.finding` `{iteration,subtask,finding_id,source_url}`, `marker.subagent.failed` `{iteration,subtask,reason}` (confirmed verbatim against merged `ActivityProjectionService.mapMarker` + `ActivityEventResponse` — `iteration`/`subtask` are typed top-level fields, rest in `details`). `buildSubagentTree` groups client-side round (`iteration`, 1-based) → sub-agent (`subtask`) → step leaves using the existing `Fold`: `activity-round-{i}-toggle` → `activity-subagent-{subtask}` (+ `-status`/`-retry` chips) → leaf rows reusing `DetailsAffordance`. Retry linkage: a subtask id that `failed` in an earlier round flags its later-round group "retried from round M". Tree renders once at the first sub-agent marker's stream position; the rest are skipped so non-sub-agent rows render unchanged (no regression). **Marker SKELETON only — no sub-agent transcript (E5).** No new API client method (markers ride the existing `listActivity`). **Tests:** `make console-test` green — **188 passed / 20 files** (was 134 before; +54: `SupervisorConfigSection.test.tsx` 19, `CreateAgentDialog.test.tsx` +6 (12 total), `ActivityPane.test.tsx` +5 (19 total), `AgentDetailPage.test.tsx` +4 (18 total) — supervisor read-only view + EDIT-mode invariant-#2 non-editable + topology/preset/supervisor not re-sent on update + react-agent section-absent). `tsc --noEmit` clean (one pre-existing unrelated unused-import error in `ContextManagementSection.test.tsx`). **Browser verification handed to orchestrator:** new **Scenario 21** added to `CONSOLE_BROWSER_TESTING.md` (Templates B+D, two-layer-naming + E5-skeleton guards) + §Coverage-Matrix row `supervisor`/`agent_mode` (Create 21 / Detail 21 read-only / Edit 21 read-only / Submit — / Task Detail 21) + §When-to-Run row. The subagent does NOT run Playwright. **Browser gate (orchestrator, 2026-06-09, Playwright vs live `make start` stack on this branch):** Scenario 1 (smoke — dashboard 5 numeric cards, routes render, 0 console errors) + Scenario 21 **steps 1–4, 7-naming, 8 PASS** — preset `<select>` (chat default, research="Deep Research"), section reveal/hide gate + all five field testids + writer-style enum labels, `POST /v1/agents` body carries `preset="research"` + `supervisor` with ONLY the set fields (`max_fanout=8`, `source_allowlist=[web_search]`, `writer_style=formal_report`; `max_iterations`/`scope_clarification` absent) + **no synthesized `topology`**, read-only-on-edit parity (preset/topology render as non-editable `<span>`/`<p>` with persisted values intact + "fixed at agent creation" note, every supervisor field `disabled`), `/supervisor/i` absent from visible copy on every surface, **0 console errors** across the full create+detail+edit flow. **Steps 5–6 (live sub-agent activity tree + failed→retry linkage) NOT run live** — they need a real multi-iteration fan-out with a round-1 failure retried in round-2 (non-deterministic, real tokens, minutes); tree grouping is deterministically covered by the 19 `ActivityPane` unit tests (incl. the two-iteration+retry fixture), and the **live-tree render is assigned to S11's E2E** (which seeds the fan-out fixture). Left a test agent `DeepResearch Browser Gate` (21aa6c84…) in the dev DB. |
| S11 | Supervisor E2E + Playwright scenario | Done | **Verification capstone — composed S1–S10 system proven end-to-end; §A12 ledger fully dispositioned; live-worker resume confirmed clean. NO source changes to S1–S10.** **(1) AC-manifest** `services/worker-service/tests/test_supervisor_ac_mapping.py` — one meta-test per behavior #1–#9 asserting the concrete test exists (mirrors `test_track5_ac_mapping.py`); #9 greps the existing **Scenario 21** heading in `CONSOLE_BROWSER_TESTING.md` (orchestrator runs Playwright; this only asserts the scenario text is present). Behaviors already covered by S6/S7/S8/S3 POINT at those tests (reconciled, not duplicated); genuinely-new composed-graph tests added for #1 event-level, #2/#3 partial-failure, #5/#6 resume-forward-vs-redrive, #7 caps. **New worker tests** (no-infra, MemorySaver, `durability="sync"`, worktree-safe): `test_supervisor_fanout_events.py` (#1), `test_supervisor_partial_failure.py` (#2/#3 composed `build_supervisor_graph`), `test_supervisor_redrive.py` (#5 reuse vs #6 recompute via LangGraph time-travel), `test_supervisor_caps.py` (#7 composed). **REST E2E** `tests/backend-integration/test_supervisor_research_e2e.py` — research-preset create (server-seeded `topology=supervisor`) → submit → poll `GET /tasks/{id}/activity` → asserts `marker.subagent.*`/`marker.supervisor.iteration` project with `iteration`/`subtask`, 3 distinct round-1 subtasks, and **ONE task row** (no sub-agent rows; Pattern-B grep clean: no `sub_agent_id`/`parent_task_id`/`waiting_for_subagent`). New `ApiClient.get_activity` helper. **(2) §A12 LEDGER — ALL THREE ROWS CLOSED (v1 sufficient)** — `test_supervisor_deferred_ledger.py` measures: **D1** max `subagent_results` payload at the research ceiling (50 verbose entries) = **0.117 MB**, well under 1 MB, `checkpoint.oversized` never fires → Closed; **D2** corpus 73 > cap 50 → cap-hit, 23 dropped + LOGGED, all 50 kept findings resolvable with byte-identical quotes (degradation = fewer, not corrupted) → Closed; **D3** boundary meter bills each fan-out branch once (S8 DB test: 5-way round = 13,260 µ$ once, no `sub_agent_id` row; overshoot bounded by `max_fanout × ceiling`), finer mid-round metering deferred post-GA → Closed. Plan §A12 table + §A8 open-list edited to record the dispositions. **(3) LIVE-WORKER RESUME — CONFIRMED CLEAN (no config-reinjection defect)** — `tests/backend-integration/test_supervisor_live_resume.py`: a `research`-preset task tripped the per-task budget pause **at the fan-out super-step boundary** (budget 9000 sits between the post-supervisor 6300 and the post-fan-out 12600 cumulative — verified the pause fires only after all 3 round-1 siblings completed + emitted findings), then `POST /resume` after a budget bump → `execute_task` rebuilt the supervisor graph + re-ran `_inject_supervisor_configurable` → resumed FORWARD to `completed` (Scope→Writer) with the round-1 siblings **restored, not re-billed** (post-resume increment < one fan-out round). The predicted `Ignoring unknown node name supervisor_fanout` / config-reinjection gap **did NOT surface** — a throwaway graph-layer spike on real Postgres confirmed the resume re-derives pending Sends cleanly and never recomputes completed siblings (the warning appears only when the supervisor *legitimately* fans out a NEW round on resume, which is correct forward progress). Plus the AC-114 wrapper check (per-inner-turn resume + namespaced transcript on real Postgres) is satisfied by S3's `test_subagent_fanout_durability.py` (2 tests, referenced in the manifest). **Counts:** `make worker-test` **1307 passed** (+3 ledger over the prior 1304); `make e2e-test` all green incl. 6 supervisor/subagent DB tests (2 new S11 + 2 S8 + 2 S3). **Defect found during S11 — now FIXED (2026-06-09):** the `iteration` field on `marker.subagent.*` events was statically 0 every round (read from once-injected `config['configurable']['iteration']`, not per-round state), so S10's Console `buildSubagentTree` (groups on `event.iteration ?? 0`) collapsed all rounds' sub-agents into "Round 0" while `marker.supervisor.iteration` carried the real round. **Fix (minimal, `executor/supervisor/graph.py`):** `_fanout_edge` — which has the live `state` with the current `iteration` minted by `supervisor_node` — now puts `iteration` in each `Send` payload, and `_fanout_node` reads `iteration = int(state.get("iteration", 0) or 0)` from the payload instead of the static config. Live state is authoritative, NOT the `"<iteration>.<index>"` subtask id (a re-dispatched subtask carries its original-round id forward for cross-round linkage, so the id's round is the first attempt, not the current one). LangGraph-mechanics confirmed against `langgraph==1.0.5` (throwaway spike): `iteration` is a `_max_reducer` channel, the payload is the branch's input only, `_fanout_node` never writes `iteration` on output → channel + iteration conditional edge unregressed. **S10 needed NO change** (already groups on `event.iteration`; the `ActivityPane` fixture already used correct per-round iterations). Tests: `test_supervisor_fanout_events.py` flipped from documenting the bug to asserting distinct live iteration per round + a re-dispatched subtask keeping its round-1 id while its round-2 marker carries iteration=2; `test_supervisor_citations.py` direct-`_fanout_node` fixture updated to pass `iteration` in the Send payload. Worker `test_supervisor_fanout*.py` (20) + no-infra supervisor suite (68) + `make e2e-test PYTEST_ARGS='-k supervisor'` (4) + `make console-test` (188) all green. **Did NOT** flip STATUS.md / archive (orchestrator-owned after the final Playwright pass); **did NOT** run Playwright/`make start`. |

## Decisions / escalations (see plan §A11) — ALL RESOLVED (2026-06-05)

Settled via six throwaway spikes (`langgraph==1.0.5`, one against real Postgres) + concrete decisions:
- **E1 — cost mechanism PROVEN (spike #6):** `event["agent"]` misses sub-agent spend (baseline=0); `subgraphs=True` + aggregate captures all of it → S8 builds this.
- **E2 — RESOLVED (spikes #1/#5):** pause at the fan-out super-step boundary; durability holds (cross-process on Postgres).
- **E3 — DECIDED:** sub-agents are headless (no `interrupt()`); multi-interrupt resume not built.
- **E4 — DECIDED:** `subagent.heartbeat` → Langfuse span event (not a `task_events` row).
- **E5 — RESOLVED:** sub-agent transcript persists in the namespaced sub-checkpoint (spikes #2/#3); Console drill-in is a read path, no new table.
- **E6 — RESOLVED (ops):** per-agent `max_concurrent_tasks=2` + worker-pool sizing guidance; per-worker supervisor cap deferred.
- **E7 — DECIDED:** `research` preset `task_timeout_seconds=14400` (4 h, tunable).
- **E8 — DECIDED:** deterministic `subtask` ids (`{iteration}.{index}`) minted in S6.
- **E10 — DECIDED:** migration `0025` uses `ADD CONSTRAINT … NOT VALID` + `VALIDATE`.

**Durability proven (spikes #2–#5):** sub-agent = checkpointed subgraph node via `Send`; per-inner-turn crash resume, dynamic-N fan-out, transcript persistence, and `dispatch_subagent`-via-`Send` threading — all verified, including cross-process on real Postgres.

**S3 verification finding (2026-06-08):** the "verify, don't assume on real Postgres" caveat caught a real gap — the repo's **custom** `PostgresDurableCheckpointer` (distinct from the spikes' `AsyncPostgresSaver`) serialises the checkpoint payload with `langchain_dumps`, which **cannot** round-trip a `Send` parked on the `__pregel_tasks` channel (it emits a `not_implemented` marker that raises on load). Without a fix the `Send`-reached fan-out resumes with `NotImplementedError`. **Fixed in S3** by routing only that channel through the JsonPlus `serde`; all other channels keep `langchain_dumps` (message round-trip invariant intact). S6's structural `Send` fan-out and S8's `Send`-wired graph both rely on this fix.

## Definition of done (track completion gate)

This track is **not "complete" / archivable** until **both** hold:
1. All tasks S1–S11 done (table above), and
2. The **Deferred Decisions Ledger** (plan [§A12](./plan.md#a12-deferred-decisions-ledger-definition-of-done-gate)) is **fully dispositioned** — every row (D1 checkpoint-size, D2 Writer reduction, D3 in-flight metering) is either **Closed** (with the metric as evidence) or **spun into a named follow-up task**. The S11 acceptance criteria force this look during E2E testing.

"v1 shipped" ≠ "fully optimized" — do not mark this track done while any §A12 row is OPEN.

**Deferred decisions outstanding:** none — D1, D2, D3 all **Closed at S11 acceptance (2026-06-09)**, v1 sufficient with the measured metric as evidence (D1 max payload 0.117 MB ≪ 1 MB; D2 cap-hit degrades to fewer-not-corrupted findings, drops logged; D3 boundary meter bills each sibling once, overshoot bounded by `max_fanout × ceiling`). See the plan's [§A12 ledger](./plan.md#a12-deferred-decisions-ledger-definition-of-done-gate) for the dispositions. Both definition-of-done conditions hold; remaining close-out is process only (final orchestrator browser pass — **done 2026-06-09/10**, STATUS.md flip, archive to `completed/`).

## Post-S11 follow-ups (not §A12 ledger items)

- **Sub-agent terminal completion marker (`subagent_completed`)** — *2026-06-13, same branch.* Closes a post-implementation visibility gap: a sub-agent that finished SUCCESSFULLY but produced **zero parseable findings** emitted nothing after `subagent_started`, so the S10 Console badge was stranded on **"running"** forever (success was only ever inferred from `subagent_finding` markers). Added the terminal success marker — the counterpart to `subagent_failed`, forming the bracket `subagent_started` → `subagent_completed` | `subagent_failed`. **Worker:** `emit_subagent_completed` (`core/subagent_events.py`, payload `{iteration, subtask}` only — no `finding_count`, §A7 minimalism) emitted on the `_fanout_node` success branch. **Migration `0026`** admits the value (25→26, reusing 0025's lock-safe `NOT VALID`→`VALIDATE`→rename recipe). **API:** `ActivityProjectionService` maps it to `marker.subagent.completed`, in `USER_VISIBLE_MARKERS` (coarse view — the sole terminal signal for a zero-finding success), **first-wins** dedup (pairs with `subagent_started`). **Console:** status unified to terminal `'done'` (renamed from `'found'`), driven by `completed` OR `finding` (finding-driven fallback survives a lost best-effort `completed` emit). **Tests:** worker `test_subagent_events.py` (+2: emit helper, 0026 admission) + `test_supervisor_fanout_events.py` (success emits completed incl. zero-finding; failure does not — mutual exclusion); Java `ActivityProjectionServiceTest` (+3: mapping/coarse-visibility, include-details filter, first-wins dedup); Console `ActivityPane.test.tsx` (+2: zero-finding success → done, completed resolves running→done). Browser: Scenario 21 §5/5d updated (chip "finding"→"done"; explicit zero-finding regression assertion) — orchestrator Playwright pass pending with the rest of the branch merge.
